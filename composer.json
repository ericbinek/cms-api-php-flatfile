{
    "name": "cms/cms-api-php-flatfile",
    "description": "Standalone schema.org-aligned CMS API on PHP — no runtime dependencies.",
    "type": "project",
    "version": "0.6.1",
    "license": "MIT",
    "authors": [
        {
            "name": "Eric Binek"
        }
    ],
    "require": {
        "php": ">=8.5"
    },
    "autoload": {
        "psr-4": {
            "Cms\\": "src/"
        }
    },
    "scripts": {
        "test": "php bin/test.php"
    }
}
